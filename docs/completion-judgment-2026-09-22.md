# 完成判定与重复纠偏修复（2026-09-22）

本轮针对任务已经完成却继续执行、或验证失败被错误解释而反复纠偏的路径做了收敛。

## 修复内容

- 纠偏进度保留有限指纹历史，识别 A→B→A 循环；不把事件编号、耗时或单纯写入次数当成进展。
- 最终回答只在 schema/纯回答纠偏中计为候选变化。代码交付验证失败时，模型换一段措辞不会延长纠偏循环。
- 测试命令退出成功但没有可确认的已执行测试时，验收状态为 unknown。它会要求补充证据或说明限制，不会伪装成测试失败并要求盲目修改代码。
- 明确的 test/cmp/diff 文件状态检查只受其目标文件影响；通用测试、构建和 lint 仍按未知传递依赖保守地要求复验。
- 完成条件仅在表达“测试通过/检查通过”等明确语义时分类为测试或诊断。create test.md 和 创建 test.md 文件 不会因为文件名产生测试义务。
- runtime 观测中的验证项现在公开 passed、failed、unknown 状态。

## 验证

- golutra-agent-runtime：266 项通过，1 项既有人工长断网 soak 忽略。
- golutra-agent-verify：24 项通过。
- 定向回归覆盖 A→B→A 纠偏、schema 回答变化、直接文件检查范围、Node 测试无执行证据和完成条件分类。
- 本地 HTTP/恢复测试在测试进程内清空大小写代理变量后通过，未改变全局代理设置。

仍然保守处理无法静态确定依赖范围的测试和构建命令；未知证据不会被当成成功或失败。该边界避免为了减少一次复验而静默复用过期结果。
